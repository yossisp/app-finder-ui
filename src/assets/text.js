const intro = 'The website serves the purpose of providing information regarding a '
  + 'certain mobile application (henceforth referred to simply as \'app\'). '
  + 'Data is provided only for the apps from App Store (iOS) or Google Play (Android). '
  + 'The website allows bulk searches of apps which can be in particular convenient '
  + 'for adtech professionals who may often '
  + 'need to search for multiple apps.';

const usage = 'To use the website simply paste the app id into the search box '
  + '(located at the top blue bar) or upload a CSV file containing app id\'s.';

const whatIsAppIdTitle = 'What is an app id?';

const sampleCSVLooksLike = 'This is how a sample CSV file looks like';

const sampleCSVdescriptionAlt = 'App Id\'s CSV File Example';

const NoTrendDataFound = 'No trend data was found for';

const SignInText = 'Sign in with Google';

export {
  intro,
  usage,
  whatIsAppIdTitle,
  sampleCSVLooksLike,
  sampleCSVdescriptionAlt,
  NoTrendDataFound,
  SignInText
};
